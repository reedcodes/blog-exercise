{%- macro postList(collection) -%}

<ul>
{%- for post in collection | reverse %}
	<li>
		{{- post.date | date -}}:
		<a href="{{- post.url -}}">{{- post.data.title -}}</a>
	</li>
{%- endfor -%}
</ul>

{%- endmacro -%}
